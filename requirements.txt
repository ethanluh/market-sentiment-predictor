# Upper bounds pin the next major so a rebuilt serving image can't silently pull
# a breaking release. Bump deliberately when upgrading.

# Data ingestion
yfinance>=0.2.40,<1.0
sec-edgar-downloader>=5.0.0,<6.0
newsapi-python>=0.2.7,<1.0
praw>=7.7.0,<8.0
pytrends>=4.9.0,<5.0
requests>=2.31.0,<3.0

# Sentiment (FinBERT)
transformers>=4.40.0,<5.0
torch>=2.2.0,<3.0

# Graph
networkx>=3.3,<4.0
scipy>=1.13.0,<2.0

# ML / prediction
scikit-learn>=1.5.0,<2.0
statsmodels>=0.14.2,<1.0
joblib>=1.4.0,<2.0

# Time series / data
pandas>=2.2.0,<3.0
numpy>=1.26.0,<3.0
pandas-market-calendars>=4.3.0,<6.0

# Serving
fastapi>=0.111.0,<1.0
uvicorn>=0.30.0,<1.0
httpx>=0.27.0,<1.0          # FastAPI TestClient

# Config
python-dotenv>=1.0.0,<2.0

# Dev / testing
pytest>=8.0.0,<9.0
pytest-cov>=5.0.0,<7.0
black>=24.0.0,<26.0
isort>=5.13.0,<7.0
mypy>=1.10.0,<2.0

# --- Optional (not required by the core pipeline) ---
# Orchestration is a lightweight in-process sequential flow (src/pipeline/run.py),
# and backtesting is a custom walk-forward harness (src/prediction/backtest.py),
# so Prefect / MLflow / vectorbt are intentionally not dependencies.
# sentence-transformers>=3.0.0   # alternative embeddings
# optuna>=3.6.0                  # hyperparameter search
# sqlalchemy>=2.0.0              # TimescaleDB/Postgres storage layer
# psycopg2-binary>=2.9.9
